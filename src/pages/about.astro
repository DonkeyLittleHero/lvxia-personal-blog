---
import BaseLayout from '../layouts/BaseLayout.astro';
import { siteConfig, topics, withBase } from '../config';
---

<BaseLayout title={`关于｜${siteConfig.name}`} description="关于驴驴小侠：在投放、经营、AI创作与代码之间持续实验。">
  <header class="page-hero container" data-reveal>
    <span class="eyebrow">ABOUT ME</span>
    <h1>你好，我是驴驴小侠。</h1>
    <p>一个同时关心生意、内容和技术的长期实践者。我喜欢把复杂问题拆开，用数据看清方向，再用 AI 与代码提高执行速度。</p>
  </header>
  <section class="section" style="padding-top: 30px">
    <div class="container">
      <div class="about-profile" data-reveal>
        <figure class="about-portrait">
          <img src={withBase(siteConfig.portrait)} alt="驴驴小侠的毕业主题艺术肖像" width="1024" height="1536" />
          <figcaption>从校园走向更大的世界，继续学习，也继续创造。</figcaption>
        </figure>
        <div class="about-profile__copy">
          <span class="eyebrow">PERSONAL LAB</span>
          <h2>这个博客，是我的公开实验记录。</h2>
          <p>我会在这里写 B 站投放、电商运营、AI 内容生产、IP 动画和编程实现。我更在意真实过程，而不是漂亮结论：为什么这样判断、怎么执行、哪里做错了，以及下一次怎样做得更好。</p>
          <p>希望这里最终不只是文章集合，而是一份持续生长的个人档案，也是一套能够反复使用的行动方法。</p>
          <div class="topic-row">{topics.map((topic) => <span class="pill">{topic.icon} {topic.label}</span>)}</div>
          <div class="contact-block">
            <span>联系与交流</span>
            {siteConfig.emails.map((email) => <a href={`mailto:${email}`}>{email}</a>)}
            <a href={siteConfig.socials.github} target="_blank" rel="noreferrer">GitHub · DonkeyLittleHero ↗</a>
          </div>
        </div>
      </div>

      <div class="timeline" data-reveal>
        <div class="timeline-item"><span>输入</span><div><strong>持续观察真实业务</strong><br />收集问题、数据、反馈与一线细节。</div></div>
        <div class="timeline-item"><span>加工</span><div><strong>拆解并做最小实验</strong><br />用一个小闭环验证，而不是停留在想象。</div></div>
        <div class="timeline-item"><span>输出</span><div><strong>写成可复用的文章与工具</strong><br />让下一次行动更快，也让经验可以被检查。</div></div>
      </div>
    </div>
  </section>
</BaseLayout>
